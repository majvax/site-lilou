/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    images: {
        unoptimized: true,
    },
    assetPrefix: '/site-lilou/',
    basePath: '/site-lilou',
};
export default nextConfig;

